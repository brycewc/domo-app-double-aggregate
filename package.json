{
  "name": "domo-app-double-aggregate",
  "version": "2.0.0",
  "description": "Horizontal bar chart overcoming Domo double-aggregation limitation",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "upload": "vite build && cd dist && domo publish && cd .."
  },
  "dependencies": {
    "ryuu.js": "^5.1.4",
    "vite": "^8.0.3"
  },
  "devDependencies": {
    "@domoinc/domo-phoenix": "^0.21.0",
    "@domoinc/ryuu-proxy": "^5.0.2"
  }
}